{
  "name": "<%= _.slugify(appName) %>",
  "version": "0.0.0",
  "dependencies": {
    "express": "3.1.0",
    "grunt": "latest",
    "grunt-contrib-copy": "latest",
    "jade": "*",
    <% if (mongo) { %>"mongodb": "0.9.9-4",<% } %>
    <% if (redis) { %>"redis": "0.7.2",<% } %>
    <% if (socketIO) { %>"socket.io": "*",<% } %>
    "stylus": "*",
    "underscore": "1.3.3",
    "underscore.string": "2.3.0"
  }
}
